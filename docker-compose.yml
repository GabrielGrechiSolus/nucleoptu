version: '3.8'

services:
  nucleoptu:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    # Uncomment and configure if you need environment variables
    # env_file:
    #   - .env.local